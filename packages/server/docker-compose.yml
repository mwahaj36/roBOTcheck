version: '3.8'

services:
  robotcheck-server:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - SECRET=rc_sec_x9y8z7w6v5u4
      - SITEKEY=rc_pub_a1b2c3d4e5f6
      - SECRETKEY=rc_sec_x9y8z7w6v5u4
      - ALLOWED_ORIGINS=http://localhost:4000
    restart: unless-stopped
